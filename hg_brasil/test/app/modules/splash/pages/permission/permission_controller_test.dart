import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:hg_brasil/app/modules/splash/splash_module.dart';

void main() {
  initModule(SplashModule());
  // PermissionController permission;
  //
  setUp(() {
    //     permission = SplashModule.to.get<PermissionController>();
  });

  group('PermissionController Test', () {
    //   test("First Test", () {
    //     expect(permission, isInstanceOf<PermissionController>());
    //   });

    //   test("Set Value", () {
    //     expect(permission.value, equals(0));
    //     permission.increment();
    //     expect(permission.value, equals(1));
    //   });
  });
}
